// Single import surface for the whole book: `#import "/template.typ": *` gives a
// chapter file the page layout (lib/book.typ), the language helpers
// (lib/lang.typ) and the table helpers (lib/tables.typ).
#import "lib/lang.typ": *
#import "lib/book.typ": *
#import "lib/tables.typ": *
